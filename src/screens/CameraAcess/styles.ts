import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        // alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'black'

    },

    cameraContainer: {
        flex: 1,
        justifyContent: 'center',
        // alignItems: 'center'
    },
    
    buttonsContainerImage:{
        flexDirection: 'row',
        justifyContent: 'space-between',
        paddingHorizontal: 50
    }

});